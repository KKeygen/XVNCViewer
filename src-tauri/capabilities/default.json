{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the VNC viewer shell",
  "windows": ["main", "session-*"],
  "permissions": [
    "core:default"
  ]
}
